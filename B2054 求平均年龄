#include <stdio.h>
int main()
{
    int n,i;
    scanf("%d",&n);
    int age[n],m=0;
    for(i=0;i<n;i++){
        scanf("%d",&age[i]);
        m=m+age[i];
    }
    float x;
    x=(float)m/n;
    printf("%.2f",x);
    return 0;
}
