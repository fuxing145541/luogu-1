#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    
    int A[n],i,sum=0;
    for(i=0;i<n;i++){
        scanf("%d",&A[i]);
        sum+=A[i];
    }

    double ave;       
    ave=(double)sum/n;
    
    printf("%d %.5lf",sum,ave);
    return 0;
}
