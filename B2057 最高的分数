#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);

    int A[n],i;
    for(i=0;i<n;i++){
        scanf("%d",&A[i]);
    }

    int max=A[0];
    for(i=1;i<n;i++){
        if(max<A[i])
        {
            max=A[i];
        }
    }
    printf("%d",max);
    
    return 0;
}
