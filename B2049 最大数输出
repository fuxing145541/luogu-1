#include <stdio.h>
int max(int x,int y)
{
    int t;
    if(x<y) {t=y;y=x;x=t;}
    return x;
}
int main(){
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    int max(int x,int y);
    printf("%d",max(a,max(b,c)));
    
    return 0;
}
